# Scoped so this agent's suite runs without touching the project's root config:
#   pytest -c crossbar_llm/pubtator3_tools/tests/pytest.ini crossbar_llm/pubtator3_tools/tests
[pytest]
asyncio_mode = auto
markers =
    live: hits the real upstream service; needs credentials
